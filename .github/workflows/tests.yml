name: Run Python Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      
language: python
python:
  - "2.7"
  - "3.7"
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover
